# tumblr-scanner runtime dependencies.
# Generated 2026-08-30 from the verified working .venv (Python 3.14.4, macOS).
# Direct deps: cdp-use, beautifulsoup4. The remainder are pinned transitive
# deps (httpx/httpcore/websockets/etc. pulled in by cdp-use) — pinned for
# reproducible external installs. Python 3.11+ recommended.
anyio==4.14.2
beautifulsoup4==4.15.0
cdp-use==1.4.5
certifi==2026.7.22
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
soupsieve==2.9.2
typing_extensions==4.16.0
websockets==17.1
